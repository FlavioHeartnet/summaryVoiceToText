{
  "name": "resumetext",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "deno run --allow-env --allow-net --allow-read --env-file=.env src/index.ts"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  },
  "private": true
}
